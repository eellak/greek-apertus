# Apertus-8B Greek CPT hyperparameters.
#
# Source this from scripts/train/train_apertus_cpt.sbatch. Keep local/private
# paths in a separate *.local.env or dataset_paths.env file.

# Local/deployment paths. Override these outside git.
APERTUS_BASE_HF="${APERTUS_BASE_HF:-}"
APERTUS_BASE_MEGATRON="${APERTUS_BASE_MEGATRON:-}"
MEGATRON_LM_DIR="${MEGATRON_LM_DIR:-}"
MEGATRON_LM_COMMIT="${MEGATRON_LM_COMMIT:-c92402e39ef3c8e69ea378a59e79059dc14541f4}"

BASE_TOKENIZER_DIR="${BASE_TOKENIZER_DIR:-$APERTUS_BASE_HF}"
EXT_TOKENIZER_DIR="${EXT_TOKENIZER_DIR:-}"
BASE_DATA_PREFIX="${BASE_DATA_PREFIX:-}"
EXT_DATA_PREFIX="${EXT_DATA_PREFIX:-$BASE_DATA_PREFIX}"

# Optimizer.
ADEMA_BETA1="0.9"
ADEMA_BETA2="0.999"
ADEMA_BETA3="0.99"
ADEMA_ALPHA="8.0"
ADEMA_WEIGHT_DECAY="0.1"
ADEMA_BETA3_WARMUP_STEPS="${ADEMA_BETA3_WARMUP_STEPS:-287}"
ADEMA_ALPHA_WARMUP_STEPS="${ADEMA_ALPHA_WARMUP_STEPS:-287}"

# Initialization and learning rate.
INIT_STD="0.008944"
LR_PEAK="1.1e-5"
LR_FINAL="1.1e-5"
LR_SCHEDULE_STYLE="${LR_SCHEDULE_STYLE:-constant}"
LR_WARMUP_INIT="${LR_WARMUP_INIT:-1.1e-6}"
LR_WARMUP_TOKENS="${LR_WARMUP_TOKENS:-1200000000}"
LR_DECAY_STYLE="${LR_DECAY_STYLE:-1-sqrt}"

GRAD_CLIP="0.1"

# Batch/sequence.
SEQ_LENGTH="4096"
MICROBATCH_SIZE="2"
GLOBAL_BATCH_SIZE="1024"
GLOBAL_BATCH_TOKENS="4194304"

# Loss.
LOSS_OBJECTIVE="${LOSS_OBJECTIVE:-goldfish}"
GOLDFISH_K="50"
GOLDFISH_H="50"

# Data semantics.
RESET_ATTENTION_MASK="${RESET_ATTENTION_MASK:-1}"
RESET_POSITION_IDS="${RESET_POSITION_IDS:-1}"
EOD_MASK_LOSS="${EOD_MASK_LOSS:-1}"
DATA_SEED="${DATA_SEED:-20260528}"
DATALOADER_WORKERS="${DATALOADER_WORKERS:-4}"

# Architecture.
MAIN_GRADS_DTYPE="fp32"
MIXED_PRECISION_FLAG="--bf16"
ACTIVATION_FLAG="--xielu"
XIELU_INIT_AP="0.8"
XIELU_INIT_AN="0.8"
QK_LAYERNORM_FLAG="--qk-layernorm"
QKNORM_IMPL="apex"
NORMALIZATION="RMSNorm"
TENSOR_MODEL_PARALLEL_SIZE="2"
PIPELINE_MODEL_PARALLEL_SIZE="1"

# Positional geometry.
MAX_POSITION_EMBEDDINGS="${MAX_POSITION_EMBEDDINGS:-65536}"
ROTARY_BASE="${ROTARY_BASE:-12000000}"
USE_ROPE_SCALING="${USE_ROPE_SCALING:-1}"
ROPE_SCALING_FACTOR="${ROPE_SCALING_FACTOR:-8.0}"

# Run length and cadence.
TRAIN_TOKENS="${TRAIN_TOKENS:-5000000000}"
SAVE_INTERVAL="${SAVE_INTERVAL:-119}"
EVAL_INTERVAL="${EVAL_INTERVAL:-25}"
EXIT_INTERVAL="${EXIT_INTERVAL:-}"
RESUME_TRAINING="${RESUME_TRAINING:-0}"
DISABLE_SAVE="${DISABLE_SAVE:-0}"

# Cluster defaults. Override at submission time.
PARTITION="${PARTITION:-normal}"
TIME_LIMIT="${TIME_LIMIT:-48:00:00}"
NODES="${NODES:-1}"
GPUS_PER_NODE="${GPUS_PER_NODE:-4}"
LAUNCH_MODE="${LAUNCH_MODE:-slurm}"
ACCOUNT="${ACCOUNT:-}"
UENV_IMAGE="${UENV_IMAGE:-}"
UENV_VIEW="${UENV_VIEW:-default}"
